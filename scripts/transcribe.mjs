#!/usr/bin/env node
/**
 * Transcribe audio/video → one vault note per recording (transcript + frontmatter).
 * Usage: node scripts/transcribe.mjs <file> [--out vault/media/audio/]
 */
console.log('Transcribe: stub. Implement transcribe → vault note; optionally trigger indexer.');
