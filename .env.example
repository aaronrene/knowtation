# Copy to .env and set your keys. Do not commit .env.
# OPENAI_API_KEY=sk-your-key-here
# Hub (Phase 11): required to run npm run hub
# HUB_JWT_SECRET=your-long-random-secret
# Optional: KNOWTATION_VAULT_PATH=/absolute/path/to/vault (or set in config/local.yaml)
